release: v0.1.23 - downsample history for long ranges

Performance:
- /gpu/history now bucket-averages rows server-side so the chart
  payload is capped at ~1800 points regardless of range. At 1Hz
  collection, 3d went from ~259k rows / ~30 MB JSON to ~1800
  rows / ~250 KB. Buckets: 1h=2s, 6h=12s, 24h=48s, 3d=144s.
  live/5m/15m stay raw. CSV export keeps full resolution.
