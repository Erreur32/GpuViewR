release: v0.9.6 fix AMD driver version display, GPU tag styling

- Fixed: AMD hosts on the default sysfs backend always showed
  "driver -" (root-caused on real hardware, /sys/module/amdgpu/version
  doesn't exist for the in-tree kernel module). Falls back to the
  kernel release.
- Changed: GPU #N is now a pill tag next to the driver version, no
  more "· driver" text.
