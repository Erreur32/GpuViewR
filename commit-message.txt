release: v0.1.28 - separate Prometheus / InfluxDB tabs, gauge style first

Changed:
- Exports & integrations now has four sub-tabs: Notification,
  Home Assistant, Prometheus, InfluxDB. The previous combined
  "Metrics" tab is split so each backend has its own page with
  its own active-dot indicator. Legacy localStorage value
  "metrics" migrates to "prometheus" on first load.
- Customize page reorders sections so "Gauge style" (arc/bar)
  is the first section, ahead of Theme and Chart palette.
