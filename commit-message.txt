release: v0.1.37 - merged dashboard header + harmonized view order

Changed:
- Dashboard collapses its two header rows into one. GPU name +
  "GPU #N · driver X.Y.Z" now share the row with the multi-GPU
  tabs (when applicable), the gauge/bar view selector, and the
  range selector. ml-auto keeps the controls right-aligned;
  flex-wrap drops them below on narrow viewports.
- System page's view selector swaps to "Gauges first, Bars
  second" to match the Dashboard layout. Keeps muscle memory
  consistent when toggling between the two pages.
