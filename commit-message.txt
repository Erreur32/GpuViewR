release: v0.1.14 - README screenshot, PNG favicon

- README: new "Screenshot" section embedding
  public/gpuviewr_screnshot.png, plus a matching link in the
  table of contents at the top of the page.
- index.html: switch the browser favicon from the small SVG to
  /gpuviewr_logo.png so the tab icon matches the in-app logo.
  The PNG is intentionally oversized for now; a properly sized
  favicon will come later.
