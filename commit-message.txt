release: v0.2.1 - collapsible alert rules, grouped GPU/System display, enhanced event visuals

Changed:
- Alerts tab: Rules section collapsed by default with rule count badge,
  auto-expands on rule create or preset install so Recent Events remain
  visible.
- Alerts tab: rules table split into GPU / System groups with category
  headers; per-rule "All GPUs" subtitle removed.
- Alerts tab: Recent Events now show metric icon, pulsing firing dot,
  colored state badge, and relative timestamps.
