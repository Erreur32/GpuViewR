release: v0.1.35 - mark ProcessCollector.cpuPrev as readonly

Changed:
- processCollector.ts: cpuPrev (per-pid CPU-time bookkeeping map)
  is now declared `readonly`. The reference is only assigned at
  construction; only map entries are mutated via set/delete.
  Resolves SonarCloud S2933 maintainability smell.
