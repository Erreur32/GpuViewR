release: v0.2.2 - grouped rules display, per-event and clear-all event deletion

Changed:
- Alerts tab: rules table split into GPU / System groups with category
  headers; per-rule "All GPUs" subtitle removed.

Added:
- Per-event delete button (hover to reveal) and "Clear all" button for
  recent events. Backend DELETE /events/:id and DELETE /events routes,
  plus AlertEventRepo.delete/deleteAll.
